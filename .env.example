# ========================================
# MIX App Digital - Environment Variables
# ========================================
# Copy this file to .env and fill in your values
# For production deployment on Railway/Render

# ========================================
# DATABASE (REQUIRED)
# ========================================
# PostgreSQL connection string
# Example: postgresql://user:password@host:5432/database
DATABASE_URL=postgresql://user:password@localhost:5432/mixapp

# ========================================
# SESSION (REQUIRED)
# ========================================
# Generate a strong random secret for sessions
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-super-secret-session-key-change-this

# ========================================
# GOOGLE OAUTH (OPTIONAL)
# ========================================
# Get credentials from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ========================================
# STRIPE PAYMENT (OPTIONAL)
# ========================================
# Get keys from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key
STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key

# ========================================
# NODE ENVIRONMENT
# ========================================
# Do NOT set NODE_ENV=production in Railway/Render environment variables
# The platforms handle this automatically during runtime
# Setting it during build will skip devDependencies and break the deployment
NODE_ENV=development

# ========================================
# IMPORTANT NOTES FOR DEPLOYMENT
# ========================================
# 1. Never commit .env files to git
# 2. Use strong, unique SESSION_SECRET in production
# 3. Use production Stripe keys (sk_live_, pk_live_) in production
# 4. Configure Google OAuth authorized origins for your domain
# 5. Railway/Render will set NODE_ENV automatically - do NOT set it manually
# 6. See DEPLOYMENT_GUIDE.md for complete instructions
